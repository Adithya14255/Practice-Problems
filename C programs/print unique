#include <stdio.h>


int main()
{
    int n=7;
    int arr[10] = {1,2,2,3,3,3,5};
    for(int i=0;i<n-1;i++){
        if(arr[i]!=arr[i+1]) printf("%d ",arr[i]);
    }
    if(arr[n-2]!=arr[n-1]) printf("%d ",arr[n-1]);
    return 0;
}
