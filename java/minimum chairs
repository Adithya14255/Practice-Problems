class Solution {
    public int minimumChairs(String s) {
        int c = 0,max = 0;
        for(int i=0;i<s.length();i++){
            if(s.charAt(i) == 'E'){
                c++;
                if (c>max) max = c;
            }
            else c--;
        }
        return max;
    }
}
