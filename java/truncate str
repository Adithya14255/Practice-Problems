class Solution {
    public String truncateSentence(String s, int k) {
        if(k == s.split(" ").length) return s;
        int end = 0;
        for(int i=0;i<s.length();i++){
            if(k == 0) break;
            if(s.charAt(i) == ' ') k--;
            end = i;
        }
        return s.substring(0,end);
    }
}
