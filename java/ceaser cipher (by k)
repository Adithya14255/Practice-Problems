class Solution {
    public String getEncryptedString(String s, int k) {
        StringBuilder out = new StringBuilder();
        for(int i=0;i<s.length();i++){
            out.append(s.charAt((i+k)%s.length()));
        }
        return out.toString();
    }
}
