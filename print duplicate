 int arr[11] = {1,0,0,2,2,1,3,3,5,2};
    int seen[100] = {0};
    int printed[100] = {0};
    for(int i=0;i<11;i++){
        if(seen[arr[i]] && !printed[arr[i]]){
            printf("%d ",arr[i]);
            printed[arr[i]] = 1;
        }
        seen[arr[i]] = 1;
    }
