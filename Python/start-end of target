class Solution:
    def searchRange(self, nums: List[int], target: int) -> List[int]:
        start = -1
        if target not in nums:
            return [-1,-1]
        l=0
        r = len(nums)-1
        while nums[l] != target:
            l+=1
        while nums[r] != target:
            r-=1
        return [l,r]
        
