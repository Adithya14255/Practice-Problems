class Solution:
    def shortestToChar(self, s: str, c: str) -> List[int]:
        arr = [i for i in range(len(s)) if s[i] == c]
        j = 0
        sol = []
        for i in range(len(s)):
            sol.append(min([abs(i-j) for j in arr]))
        return sol
